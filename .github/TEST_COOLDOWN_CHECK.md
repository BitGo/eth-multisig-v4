# Test PR — dependency cooldown check

Dummy PR to verify the "Enforce Cooldown Period" check (INF-2235) fires
correctly via `ci-public-proxy` when `push.yml` runs on a pull request.

Safe to close/delete without merging once verified.
